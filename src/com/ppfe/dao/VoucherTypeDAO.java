package com.ppfe.dao;

import javax.ejb.Stateless;
import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;

import com.ppfe.entities.VoucherType;

@Stateless
public class VoucherTypeDAO {
/*	// Injection of manager who makes the connection with the database

	@PersistenceContext(unitName = "db_ppfe_pu_voucher_type")
	private EntityManager em;

	public void create(VoucherType voucherType) throws DAOException {
		try {
			em.persist(voucherType);
		} catch (Exception e) {
			throw new DAOException(e);
		}
	}*/
}
 